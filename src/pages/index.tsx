import React from 'react'
import styled from '@emotion/styled'

function index() {
  return <IndexBlock>Index</IndexBlock>
}

const IndexBlock = styled.div`
  text-align: center;
`

export default index
